<%# app/views/posts/index.html.erb %>
<div class="min-h-screen w-full relative font-body">

  <!-- 背景装飾 -->
  <div class="fixed top-0 left-0 w-full h-full overflow-hidden -z-10 pointer-events-none">
    <div class="absolute top-[-10%] left-[-10%] w-[500px] h-[500px] bg-blue-400/20 rounded-full blur-[100px] animate-pulse"></div>
    <div class="absolute bottom-[-10%] right-[-10%] w-[600px] h-[600px] bg-cyan-400/20 rounded-full blur-[120px] animate-pulse" style="animation-duration: 4s;"></div>
  </div>

  <main class="container mx-auto px-4 py-8 max-w-2xl relative z-10">
    <!-- ヘッダーエリア -->
    <div class="flex flex-col items-center justify-center mb-8 text-center">
      <h1 class="text-4xl md:text-5xl font-black text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-cyan-500 dark:from-blue-400 dark:to-cyan-300 mb-2 tracking-tight drop-shadow-sm">
        みんなの足跡
      </h1>
      <p class="text-gray-500 dark:text-gray-400 font-medium">
        今日の散歩、誰かの発見。
      </p>

      <!-- タブ切り替え -->
      <div class="mt-6 p-1 bg-white/50 dark:bg-slate-800/50 backdrop-blur-md rounded-full inline-flex shadow-sm border border-white/20">
        <%= link_to posts_path, class: "px-6 py-2 rounded-full text-sm font-bold transition-all duration-300 #{current_page?(posts_path) ? 'bg-gradient-to-r from-blue-500 to-cyan-400 text-white shadow-md transform scale-105' : 'text-gray-600 dark:text-gray-300 hover:bg-white/50 dark:hover:bg-slate-700/50'}" do %>
          <span class="flex items-center gap-2">
            <span class="material-symbols-outlined text-lg">public</span>
            みんな
          </span>
        <% end %>

        <%= link_to mine_posts_path, class: "px-6 py-2 rounded-full text-sm font-bold transition-all duration-300 #{current_page?(mine_posts_path) ? 'bg-gradient-to-r from-blue-500 to-cyan-400 text-white shadow-md transform scale-105' : 'text-gray-600 dark:text-gray-300 hover:bg-white/50 dark:hover:bg-slate-700/50'}" do %>
          <span class="flex items-center gap-2">
            <span class="material-symbols-outlined text-lg">history</span>
            自分
          </span>
        <% end %>
      </div>
    </div>

    <!-- インライン風投稿トリガー（Claymorphism Style） -->
    <div class="mb-8">
      <div class="bg-[#f0f4f8] dark:bg-slate-800 rounded-[2rem] p-6 cursor-pointer transition-all duration-300 group hover:-translate-y-1"
           style="box-shadow: 8px 8px 16px rgba(166, 175, 195, 0.4), -8px -8px 16px rgba(255, 255, 255, 0.8);"
           data-controller="modal"
           data-modal-dialog-id-value="new_post_modal"
           data-action="click->modal#open">

        <div class="flex items-center gap-4">
          <!-- 自分のアバター（浮き出し） -->
          <div class="w-14 h-14 rounded-2xl bg-gray-200 dark:bg-gray-700 overflow-hidden flex-shrink-0 border-2 border-white dark:border-slate-600"
               style="box-shadow: 4px 4px 8px rgba(166,175,195,0.2), -4px -4px 8px rgba(255,255,255,0.8);">
            <% if current_user.avatar_url.present? %>
              <%= image_tag current_user.avatar_url, class: "w-full h-full object-cover", alt: current_user.name %>
            <% else %>
              <div class="flex items-center justify-center w-full h-full text-gray-500 font-bold text-lg">
                <%= current_user.name[0].upcase %>
              </div>
            <% end %>
          </div>

          <!-- 入力風エリア（くぼみ） -->
          <div class="flex-1 bg-gray-100 dark:bg-slate-900/50 rounded-full px-6 py-4 text-gray-500 dark:text-gray-400 text-sm group-hover:bg-gray-200/80 dark:group-hover:bg-slate-800 transition-colors flex items-center justify-between shadow-inner border border-black/5 dark:border-white/5">
            <span class="font-medium">今日の散歩の気分をシェアしよう...</span>
            <span class="material-symbols-outlined text-gray-400 group-hover:text-blue-500 transition-colors">edit_note</span>
          </div>

          <!-- アイコン類（飾り・Claymorphism） -->
          <div class="hidden sm:flex gap-3 ml-2">
            <div class="w-10 h-10 rounded-full bg-orange-50 dark:bg-orange-900/20 flex items-center justify-center text-orange-500 shadow-sm border border-orange-100 dark:border-orange-800/50 group-hover:scale-110 transition-transform duration-300"
                 style="box-shadow: 3px 3px 6px rgba(166,175,195,0.2), -3px -3px 6px rgba(255,255,255,0.8);">
              <span class="material-symbols-outlined text-xl">wb_sunny</span>
            </div>
            <div class="w-10 h-10 rounded-full bg-green-50 dark:bg-green-900/20 flex items-center justify-center text-green-500 shadow-sm border border-green-100 dark:border-green-800/50 group-hover:scale-110 transition-transform duration-300 delay-75"
                 style="box-shadow: 3px 3px 6px rgba(166,175,195,0.2), -3px -3px 6px rgba(255,255,255,0.8);">
              <span class="material-symbols-outlined text-xl">sentiment_satisfied</span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- 投稿一覧エリア -->
    <div class="space-y-6 pb-24">
      <!-- 上部ページネーション -->
      <div class="flex justify-center mb-4">
        <%= paginate @posts %>
      </div>

      <% if @posts.any? %>
        <%= render partial: "post", collection: @posts %>
      <% else %>
        <!-- 空の状態（Empty State） -->
        <div class="flex flex-col items-center justify-center py-20 text-center bg-white/40 dark:bg-slate-800/40 backdrop-blur-sm rounded-[2.5rem] border-2 border-dashed border-gray-300 dark:border-gray-700">
          <div class="text-6xl mb-4 animate-bounce">🚶‍♂️</div>
          <h3 class="text-xl font-bold text-gray-700 dark:text-gray-200 mb-2">まだ投稿がありません</h3>
          <p class="text-gray-500 dark:text-gray-400">
            一番乗りで、今日の散歩の気分を<br>シェアしてみませんか？
          </p>
        </div>
      <% end %>

      <!-- ページネーション -->
      <div class="mt-10 flex justify-center">
        <%= paginate @posts %>
      </div>
    </div>
  </main>

  <!-- 新規投稿モーダル（Claymorphism Style） -->
  <dialog id="new_post_modal"
          class="p-0 rounded-[2.5rem] shadow-2xl backdrop:bg-gray-900/40 backdrop:backdrop-blur-sm bg-transparent open:animate-zoom-in"
          data-controller="modal"
          data-modal-target="dialog">
    <div class="bg-[#f8fafc] dark:bg-slate-800 w-full max-w-lg p-8 rounded-[2.5rem] border-4 border-white dark:border-slate-700"
         style="box-shadow: 20px 20px 60px #d1d9e6, -20px -20px 60px #ffffff;">

      <div class="flex justify-between items-center mb-6">
        <h3 class="text-2xl font-black text-gray-800 dark:text-white flex items-center gap-3">
          <span class="text-3xl filter drop-shadow-sm">👟</span>
          <span class="bg-clip-text text-transparent bg-gradient-to-r from-blue-600 to-cyan-500">てくてくシェア</span>
        </h3>
        <button type="button"
                data-controller="modal"
                data-action="click->modal#close"
                class="w-10 h-10 rounded-full flex items-center justify-center bg-gray-100 dark:bg-slate-700 text-gray-400 hover:text-red-500 hover:bg-red-50 transition-all shadow-sm hover:shadow-inner outline-none focus:ring-2 focus:ring-gray-200 dark:focus:ring-slate-600">
          <span class="material-symbols-outlined font-bold">close</span>
        </button>
      </div>

      <!-- フォームのレンダリング -->
      <%= render 'form', post: @post %>
    </div>
  </dialog>
</div>

<style>
  /* dialogのアニメーション用 */
  dialog[open] {
    animation: zoomIn 0.3s ease-out;
  }
  @keyframes zoomIn {
    from { opacity: 0; transform: scale(0.95); }
    to { opacity: 1; transform: scale(1); }
  }
</style>
